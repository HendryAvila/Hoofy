## Tasks

### TASK-001: Rewrite Quick Start section of README.md
- [ ] Step 1 "Install" shows platform tabs: macOS (Homebrew), Linux, Windows, with Go/source in details
- [ ] Step 2 "Connect to your AI tool" clearly separates MCP Server (the binary) from Plugin (Claude Code only enhancement)
- [ ] Add `claude mcp add` command prominently for Claude Code MCP setup
- [ ] Explain: MCP = the server (works everywhere), Plugin = agent + skills + hooks (Claude Code only bonus)
- [ ] Keep all existing tool configs (Cursor, VS Code, OpenCode, Gemini)
